#include "main.h"

/**
*_abs - computes the absolute value of an integer.
*@x - value
*Return: Always
*/

int _abs(int x)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0);
	{
		return (-n);
	}
	else
	{
		return (0);
	}
}
